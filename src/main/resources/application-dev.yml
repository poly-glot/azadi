spring:
  thymeleaf:
    cache: false
  cloud:
    gcp:
      datastore:
        emulator:
          enabled: true

server:
  servlet:
    session:
      cookie:
        secure: false

azadi:
  encryption-key: dev-only-key-change-in-prod-32ch
  encryption-salt: a1b2c3d4e5f6a7b8
